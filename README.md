Install via the Ableton Extensions panel in Ableton

Right-click in the lane to add a midi clip a certain length without looping, or choose `Create Midi Clip...` to load the UI with some options.
Right-click on an instrument/midi track to change panning options with some quick presets.
